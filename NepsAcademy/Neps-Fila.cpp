#include <bits/stdc++.h>

using namespace std;

int main(){
	int n, q;
	vector<int> v;

	for(int i = 0; i < n; i++){
		int x;
		cin >> x;
		v.push_back(x);
	}


	vector<int> dec;

	dec.push_back(v[n-1]);

	for(int i = n-2; i >= 0; i++){
		if(dec.)
	}
	


	while(q > 0){
		int t, h, pos;
		cin >> t >> h >> pos;

		if(t == 0) v.insert(pos, h);
		else{
			
		}
	}
}